class PlayerMessage {
  constructor(username, height, width, x, y) {
    this.username = username;
    this.height = height;
    this.width = width;
    this.x = x;
    this.y = y;
  }
}

module.exports = PlayerMessage;
