{
  "name": "onlinelounge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "dev": "cd public && npx webpack serve --mode development --port 8080",
    "dev:local": "cd public && NODE_ENV=development WORLDSERVER_URL=ws://localhost:7778 npx webpack serve --mode development --port 8080",
    "build": "cd public && npx webpack --mode production && cd ..",
    "build:dev": "cd public && npx webpack --mode development && cd ..",
    "deploy": "npm run build && ./deploy.sh",
    "deploy:no-invalidate": "npm run build && ./deploy.sh --no-invalidate"
  },
  "dependencies": {
    "matter-js": "^0.17.1",
    "tmx-parser": "^1.5.0",
    "uuid": "^8.3.2"
  },
  "devDependencies": {
    "jasmine": "^4.0.2",
    "webpack": "^5.105.0",
    "webpack-cli": "^6.0.1",
    "webpack-dev-server": "^5.0.0"
  }
}
